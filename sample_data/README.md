# Synthetic sample data

These files are **synthetic test fixtures**, not captured Nissan traffic.

CAN IDs 0x120 and 0x351 are arbitrary test identifiers and MUST NOT be interpreted as Nissan ECU/message mappings.

The fault fixture intentionally inserts a long timing gap for 0x351 so the anomaly pipeline can be tested deterministically.
